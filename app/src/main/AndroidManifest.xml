<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aware.plugin.SmartVolume">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/Theme.Aware">

        <service android:name="com.aware.plugin.SmartVolume.Plugin" android:exported="true" />
        <activity android:name="com.aware.plugin.SmartVolume.Settings" android:exported="true" android:theme="@style/Theme.Aware" />

    </application>

</manifest>
